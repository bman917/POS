<nav class="top-bar" data-topbar role="navigation" data-options="is_hover: false">
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to "POS", main_app.portal_index_path%></h1>
    </li>
  </ul>

<%=render 'user_manager/session_details'%>

<% if user_signed_in? %>
<section id="admin" class="top-bar-section">
  <ul class="right">
    <li class="has-dropdown">
      <%=link_to 'Admin', "#"%>
        <ul class="dropdown">
          
          <li><%=link_to 'Manage Users', user_manager.users_path%></li>

          <li class="has-dropdown">
            <a href="#">Item Configuration</a>
            <ul class="dropdown">
              <li><%=link_to 'Attributes', main_app.attribs_path%></li>
              <li><%=link_to 'Categories', main_app.categories_path%></li>
              <li><%=link_to 'Units', main_app.units_path%></li>
            </ul>
          </li>

          <li><%=link_to 'Suppliers', main_app.suppliers_path%></li>
          <li class="has-dropdown">
            <%=link_to 'Items', main_app.items_path%>
            <ul class="dropdown">
              <li><%=link_to 'Show Item List', main_app.items_path%></li>
              <li><%=link_to 'Add New', main_app.new_item_path%></li>
            </ul>
          </li>

        </ul>
    </li>
  </ul>
</section>
<% end %>
</nav>

<%=javascript_tag do %>
//Remove all links from tab cycle
$(document).ready(function(){
  $('nav a').attr("tabindex", -1);
});
<% end %>

<div id="modal_form" class="reveal-modal" data-reveal>
</div>
