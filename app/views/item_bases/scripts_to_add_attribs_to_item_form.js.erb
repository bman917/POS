$("tr.attrib_row").hide('slow',function(){ 
  $(this).remove(); 
});

<% @attribs.each do | attrib | %>
  $('#item_display').append("<%=j render partial: '/items/attrib_input_row', locals: {attrib: attrib, value: ""}%>");
<% end if @attribs %>
