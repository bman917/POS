<div class="row">
<div class="small-12 columns">
<%= form_for(@attrib) do |f| %>
  <% if @attrib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attrib.errors.count, "error") %> prohibited this attrib from being saved:</h2>

      <ul>
      <% @attrib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<fieldset>
  <legend>Item Attribute</legend>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :display_number%><br>
    <%= f.number_field :display_number%>
  </div>
  <div class="actions">
     <%= f.submit value: 'Save', class: 'button expand' %>
  </div>
</fieldset>
<% end %>
</div>
<div>